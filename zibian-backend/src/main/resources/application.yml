spring:
  application:
    name: zibian-backend
  datasource:
    url: jdbc:postgresql://10.132.152.2:6111/zibian
    username: postgres
    password: Aa123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: 10.132.152.2
      port: 6389
      password: difyai123456

server:
  port: 8570

# 数据初始化配置
data:
  initialization:
    enabled: false  # 是否启用数据初始化
    update-character-data: false  # 是否更新汉字数据（从API获取）


